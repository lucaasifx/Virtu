import React from 'react';
import { WorkoutCountdown } from '@/components/features/Workout/Countdown/WorkoutCountdown';

export default function FinishSelectionScreen() {
    return <WorkoutCountdown />;
}
